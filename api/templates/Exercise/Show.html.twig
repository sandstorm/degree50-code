{% extends 'Base.html.twig' %}

{% block title %}{{ exercise.name }}{% endblock %}

{% block body %}
    {{ include('Exercise/Pagination.html.twig', {exercise: exercise, currentPhase: currentPhase, amountOfPhases: amountOfPhases}) }}
    <h2>{{ exercise.name }}</h2>
    {{ phase.definition }}
    <div data-controller="react" data-react-widget="ShowExercisePhase" data-react-props="{{ phase.definition }}">
        Loading react part...
    </div>

    {{ include('Exercise/Pagination.html.twig', {exercise: exercise, currentPhase: currentPhase, amountOfPhases: amountOfPhases}) }}
{% endblock %}
