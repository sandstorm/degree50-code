{% extends 'BaseFullscreen.html.twig' %}

{% block title %}Aufgabe bearbeiten{% endblock %}

{% block body %}
    <div class="container">
        <header class="content-header content-header--center">
            <h3>Aufgabe bearbeiten</h3>
        </header>
    </div>

    <div class="new-exercise-form">
        {{ form_start(form) }}
            <div class="container">
                {{ form_errors(form) }}

                {{ form_row(form.name, { 'label': 'Titel der Aufgabe' }) }}
                {{ form_row(form.description, { 'label': 'Lernauftrag / Beschreibung' }) }}
            </div>


            <footer class="form-footer">
                <a class="btn btn-outline-primary" href="{{ path('app_exercise-overview-show-course', {id: exercise.course.id}) }}">Zurück zur Übersicht</a>
                {{ form_row(form.save, { 'label': 'Aufgabe speichern' }) }}
            </footer>
        {{ form_end(form) }}
    </div>

{% endblock %}
