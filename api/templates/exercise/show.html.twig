{% extends 'base.html.twig' %}

{% block title %}{{ exercise.name }}{% endblock %}

{% block body %}
    {{ include('exercise/pagination.html.twig', {exercise: exercise, currentPhase: currentPhase, amountOfPhases: amountOfPhases}) }}
    <h2>{{ exercise.name }}</h2>
    {{ phase.definition }}
    <div data-react-widget="ShowExercisePhase" data-react-props="{{ phase.definition|json_encode|e('html_attr') }}">
    </div>

    {{ include('exercise/pagination.html.twig', {exercise: exercise, currentPhase: currentPhase, amountOfPhases: amountOfPhases}) }}
{% endblock %}
