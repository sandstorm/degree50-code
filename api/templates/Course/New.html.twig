{% trans_default_domain 'forms' %}
{% extends 'BaseFullscreen.html.twig' %}

{% block title %}{% trans %}course.new.title{% endtrans %}{% endblock %}

{% block body %}
    <div class="exercise-form">
        <div class="container">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('exercise-overview') }}">Kurs-Übersicht</a>
                    </li>
                    <li class="breadcrumb-item active"
                        aria-current="page">{% trans %}course.new.title{% endtrans %}</li>
                </ol>
            </nav>
            <header class="content-header">
                <h3>{% trans %}course.new.title{% endtrans %}</h3>
            </header>

            {{ form_start(form) }}
            {{ form_errors(form) }}

            {{ form_row(form.name) }}
            {{ form_row(form.fachbereich) }}
            {{ form_row(form.users, {'required': true}) }}
        </div>
    </div>

    <footer class="footer">
        <a class="btn btn-outline-primary footer__left"
           href="{{ path('exercise-overview') }}">{% trans %}course.new.back{% endtrans %}</a>
        <div class="footer__right">
            {{ form_row(form.save) }}
        </div>
    </footer>
    {{ form_end(form) }}
{% endblock %}
