{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string"
    },
    "method": {
      "description": "The User can be completely deleted (all data of the user is removed) or anonymized (used educational content will persist).",
      "type": "string",
      "enum": ["deleted", "anonymized"]
    }
  },
  "required": [
    "userId",
    "method"
  ],
  "additionalProperties": false
}
