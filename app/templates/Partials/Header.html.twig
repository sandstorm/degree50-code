{% set route = app.request.get('_route') %}

<header class="main-header">
    {{ include('Partials/Navigation.html.twig', {
        type: 'mobile'
    }) }}
</header>
<div class="navigation__image__container">
    <img class="navigation__image" src="{{ asset('build/images/background.png') }}" alt="" />
</div>
{{ include('Partials/Navigation.html.twig', {
    type: 'desktop'
}) }}

