{% extends 'BaseNoHeader.html.twig' %}

{% block body %}
    <div class="container">
        <header class="content-header">
            <h1>{{ "security.termsOfUse.headline"|trans({}, 'base') }}</h1>
        </header>
        {{ include('Partials/TermsOfUseContent.html.twig') }}
    </div>
    <footer class="footer footer--center">
        <a class="button button--type-primary" href="{{ path('app_terms-of-use', {accepted: true}) }}" role="link">{{ "security.termsOfUse.accept"|trans({}, 'base') }}</a>
    </footer>
{% endblock %}

