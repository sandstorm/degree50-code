{% extends 'Base.html.twig' %}

{% block title %}{{ "common.degree"|trans({}, 'base') }} - {{ "security.403.accessDenied"|trans({}, 'base') }}{% endblock %}

{% block body %}
    <header class="content-header">
        <h3>{{ "security.403.accessDenied"|trans({}, 'base') }}</h3>
    </header>

    <p>
        {{ "security.403.info"|trans({}, 'base') }}
    </p>
{% endblock %}
