{% extends 'BaseNoHeader.html.twig' %}

{% block title %}{{ "common.imprint"|trans({}, 'base') }}{% endblock %}

{% block body %}
    <div class="container">
        <header class="content-header">
            <h1>{{ "security.imprint.headline"|trans({}, 'base') }}</h1>
        </header>
        {{ include('Partials/ImprintContent.html.twig') }}
    </div>
{% endblock %}
